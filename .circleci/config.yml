# Python CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-python/ for more details
#
version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6.1

    working_directory: ~/repo

    steps:
      - checkout

      - run:
          name: install dependencies
          command: |
            bash .circleci/deps.sh

      - run:
          name: build steps
          command: |
            bash .circleci/builds.sh
            
      - run: 
          name: background server for tests
          command: |
            source env/bin/activate
            python std-api-server/server.py &
            wget --retry-connrefused --waitretry=1 --read-timeout=20 --timeout=15 -t 10 http://localhost:5555
          background: true
        
      - run: 
          name: server tests
          command: bash std-api-server/test.sh
